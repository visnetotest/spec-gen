# API Specification

> Generated by spec-gen v1.0.0 on 2026-03-08 16:15

## Purpose

This document specifies the HTTP API exposed by the system.

## Requirements

### Requirement: APIAuthentication

The API SHALL require authentication via: None, API Key (ANTHROPIC_API_KEY or OPENAI_API_KEY), Bearer JWT, Not applicable

#### Scenario: AuthenticatedRequest
- **GIVEN** a request with valid authentication credentials
- **WHEN** the request is processed
- **THEN** the request is authenticated successfully

#### Scenario: UnauthenticatedRequest
- **GIVEN** a request without authentication
- **WHEN** accessing a protected endpoint
- **THEN** the response status is 401 Unauthorized

### Requirement: Functionspecification

The API SHALL support `Function specGenGenerate` to generate openspec specification files from analysis results using llm.

**Request:**

```json
{
  "options": {
    "type": "GenerateApiOptions",
    "description": "Options for generating API specifications"
  }
}
```

**Response:**

```json
{
  "type": "Promise<GenerateResult>",
  "description": "Result of the generation process"
}
```

#### Scenario: GenerateSpecifications
- **GIVEN** Valid analysis results and configuration
- **WHEN** specGenGenerate is called
- **THEN** OpenSpec specification files are generated and returned

### Requirement: Postspecification

The API SHALL support `POST /api/drift` to detect specification drift in a project.

**Request:**

```json
{
  "options": {
    "baseRef": "string",
    "headRef": "string",
    "configPath": "string",
    "logPath": "string",
    "llmService": "string",
    "threshold": "number"
  }
}
```

**Response:**

```json
{
  "driftResults": {
    "gaps": [
      {
        "filePath": "string",
        "domains": [
          "string"
        ],
        "severity": "number",
        "message": "string"
      }
    ],
    "staleSpecs": [
      {
        "filePath": "string",
        "domains": [
          "string"
        ],
        "message": "string"
      }
    ],
    "uncoveredFiles": [
      {
        "filePath": "string",
        "domains": [
          "string"
        ],
        "severity": "number",
        "message": "string"
      }
    ],
    "orphanedSpecs": [
      {
        "filePath": "string",
        "domains": [
          "string"
        ],
        "message": "string"
      }
    ],
    "adrGaps": [
      {
        "adrId": "string",
        "domains": [
          "string"
        ],
        "message": "string"
      }
    ],
    "adrOrphaned": [
      {
        "adrId": "string",
        "domains": [
          "string"
        ],
        "message": "string"
      }
    ]
  },
  "logs": [
    "string"
  ]
}
```

#### Scenario: DetectDriftInAProject
- **GIVEN** A project with specifications and code changes
- **WHEN** POST /api/drift is called with valid options
- **THEN** 200 OK with drift results and logs in response body

#### Scenario: DetectDriftWithLlmEnhancement
- **GIVEN** A project with specifications and code changes, and an LLM service configured
- **WHEN** POST /api/drift is called with valid options and LLM service
- **THEN** 200 OK with enhanced drift results and logs in response body

### Requirement: Getspecrequirement

The API SHALL support `GET /api/specs/requirements` to read spec requirements referenced in mapping.json.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "generatedAt": "string",
  "requirements": "Record<string, SpecRequirement>"
}
```

#### Scenario: GetSpecRequirements
- **GIVEN** Valid mapping.json file exists
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with generatedAt and requirements in response body

#### Scenario: GetSpecRequirementsWithMissingMapping.json
- **GIVEN** mapping.json file does not exist
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with empty requirements object in response body

#### Scenario: GetSpecRequirementsWithInvalidMapping.json
- **GIVEN** mapping.json file exists but cannot be read/parsed
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with empty requirements object in response body

### Requirement: Postcodebase

The API SHALL support `POST /api/analyze` to run static analysis on the codebase.

**Request:**

```json
{
  "rootPath": "string",
  "maxFiles": "number",
  "excludePatterns": "string[]",
  "force": "boolean",
  "outputPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "repoMap": "RepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalyzeResult['artifacts']",
  "duration": "number"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** Valid codebase with spec-gen configuration
- **WHEN** POST /api/analyze is called with valid options
- **THEN** 200 OK with analysis results in response body

#### Scenario: AnalyzeCodebaseWithRecentAnalysis
- **GIVEN** Valid codebase with recent analysis results
- **WHEN** POST /api/analyze is called with force: false
- **THEN** 200 OK with existing analysis results in response body

#### Scenario: AnalyzeCodebaseWithoutConfiguration
- **GIVEN** Valid codebase without spec-gen configuration
- **WHEN** POST /api/analyze is called
- **THEN** 400 Bad Request with error message

### Requirement: Functionspecgen

The API SHALL support `Function specGenInit(options: InitApiOptions = {})` to initialize spec-gen in a project directory.

**Request:**

```json
{
  "options": "InitApiOptions"
}
```

**Response:**

```json
{
  "result": "InitResult"
}
```

#### Scenario: InitializeSpecGen
- **GIVEN** A project directory
- **WHEN** specGenInit is called
- **THEN** spec-gen is initialized in the project directory
- **AND** A promise resolving to InitResult is returned

### Requirement: Functionspecgen

The API SHALL support `Function specGenRun(options: RunApiOptions = {})` to run the full spec-gen pipeline: init → analyze → generate.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "projectPath": {
        "type": "string",
        "description": "Path to the project to analyze"
      },
      "configPath": {
        "type": "string",
        "description": "Path to the spec-gen configuration file"
      },
      "outputPath": {
        "type": "string",
        "description": "Path to the output directory"
      },
      "verbose": {
        "type": "boolean",
        "description": "Whether to output verbose logs"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the spec-gen pipeline completed successfully"
    },
    "message": {
      "type": "string",
      "description": "A message describing the result of the spec-gen pipeline"
    },
    "logs": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Log messages generated during the spec-gen pipeline"
      }
    }
  }
}
```

#### Scenario: RunSpecGenPipeline
- **GIVEN** A project path and optional configuration
- **WHEN** The specGenRun function is called
- **THEN** The spec-gen pipeline is executed and a result object is returned

### Requirement: Postverification

The API SHALL support `POST /api/verify` to verify generated specs against actual source code.

**Request:**

```json
{
  "rootPath": "string",
  "samples": "number",
  "threshold": "number",
  "provider": "string",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "onProgress": "function"
}
```

**Response:**

```json
{
  "report": "object",
  "duration": "number"
}
```

#### Scenario: SuccessfulVerification
- **GIVEN** Valid configuration, specs, and analysis
- **WHEN** POST /api/verify is called with valid parameters
- **THEN** 200 OK with verification report and duration in response body

#### Scenario: MissingConfiguration
- **GIVEN** No spec-gen configuration
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingSpecs
- **GIVEN** No generated specs
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingAnalysis
- **GIVEN** No analysis data
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingLlmApiKey
- **GIVEN** No LLM API key
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: NoVerificationCandidates
- **GIVEN** No suitable verification candidates
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

### Requirement: Postrun

The API SHALL support `POST /api/run` to [partial spec — file too large to fully analyze (4 parts)] run the full pipeline: init → analyze → generate in sequence.

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "model": "string",
  "dryRun": "boolean",
  "yes": "boolean",
  "maxFiles": "number",
  "adr": "boolean"
}
```

**Response:**

```json
{
  "metadata": {
    "version": "string",
    "timestamp": "string",
    "duration": "number",
    "steps": {
      "init": {
        "status": "string",
        "reason": "string"
      },
      "analyze": {
        "status": "string",
        "reason": "string",
        "filesAnalyzed": "number"
      },
      "generate": {
        "status": "string",
        "reason": "string",
        "specsGenerated": "number"
      }
    },
    "result": "string",
    "error": "string"
  }
}
```

#### Scenario: RunFullPipeline
- **GIVEN** A project with valid configuration
- **WHEN** POST /api/run is called with valid parameters
- **THEN** 200 OK with run metadata in response body

#### Scenario: SkipUnnecessarySteps
- **GIVEN** A project with existing analysis
- **WHEN** POST /api/run is called with reanalyze set to false
- **THEN** 200 OK with run metadata indicating skipped steps

### Requirement: ClispecificationGenerationPipeline

The API SHALL support `CLI spec-gen run` to [partial spec — file too large to fully analyze (4 parts)] run the full spec-gen pipeline (init → analyze → generate)

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "model": "string",
  "dryRun": "boolean",
  "yes": "boolean",
  "maxFiles": "number",
  "adr": "boolean"
}
```

**Response:**

```json
{
  "version": "string",
  "timestamp": "string",
  "duration": "number",
  "steps": {
    "init": {
      "status": "string",
      "reason": "string"
    },
    "analyze": {
      "status": "string",
      "reason": "string",
      "filesAnalyzed": "number"
    },
    "generate": {
      "status": "string"
    }
  },
  "result": "string"
}
```

#### Scenario: RunFullPipeline
- **GIVEN** A project directory
- **WHEN** spec-gen run is executed
- **THEN** The pipeline runs initialization, analysis, and generation steps

#### Scenario: ForceReinitialization
- **GIVEN** A project directory with existing configuration
- **WHEN** spec-gen run --force is executed
- **THEN** The pipeline reinitializes the configuration and runs analysis and generation steps

#### Scenario: ForceFreshAnalysis
- **GIVEN** A project directory with recent analysis
- **WHEN** spec-gen run --reanalyze is executed
- **THEN** The pipeline performs a fresh analysis and runs generation steps

#### Scenario: DryRun
- **GIVEN** A project directory
- **WHEN** spec-gen run --dry-run is executed
- **THEN** The pipeline shows what would be done without making changes

### Requirement: Postanalysis

The API SHALL support `POST /api/analysis` to [partial spec — file too large to fully analyze (4 parts)] run codebase analysis to generate dependency graph and artifacts.

**Request:**

```json
{
  "rootPath": "string",
  "analysisPath": "string",
  "maxFiles": "number",
  "include": "string[]",
  "exclude": "string[]"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "llmContext": "object",
  "depGraph": "object",
  "age": "number",
  "filesAnalyzed": "number",
  "clusterCount": "number",
  "domains": "string[]"
}
```

#### Scenario: RunAnalysis
- **GIVEN** Valid rootPath and analysisPath
- **WHEN** POST /api/analysis is called
- **THEN** 200 OK with analysis data in response body

### Requirement: Postgeneration

The API SHALL support `POST /api/generation` to [partial spec — file too large to fully analyze (4 parts)] run llm generation pipeline to create specifications.

**Request:**

```json
{
  "repoStructure": "object",
  "llmContext": "object",
  "depGraph": "object",
  "model": "string",
  "outputDir": "string",
  "saveIntermediate": "boolean",
  "generateADRs": "boolean"
}
```

**Response:**

```json
{
  "specsGenerated": "number",
  "filesWritten": "string[]",
  "adrs": "object[]"
}
```

#### Scenario: RunGeneration
- **GIVEN** Valid analysis data and API key
- **WHEN** POST /api/generation is called
- **THEN** 200 OK with generation report in response body

### Requirement: Configureserverserver

The API SHALL support `configureServer src/cli/commands/view.ts` to configures the development server for the application.

#### Scenario: ConfigureServer
- **GIVEN** Development server instance
- **WHEN** configureServer is called
- **THEN** Server is configured with necessary middleware and routes
