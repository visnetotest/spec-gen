# System Overview

> Generated by openlore v1.0.0 on 2026-04-05 10:50
> Confidence: 95%

## Purpose

OpenSpec is a CLI tool designed to generate and analyze OpenSpec specifications from codebases. It
helps developers create comprehensive documentation by extracting and organizing information from
source code, making it easier to understand and maintain software projects.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes codebase functions to identify potential refactoring opportunities based on structural metrics and architectural patterns. | [spec.md](../analyzer/spec.md) |
| Api | Generates OpenSpec specification files from analysis results using LLM. | [spec.md](../api/spec.md) |
| Services | Manages reading, writing, and merging configuration files for openlore and OpenSpec. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates OpenSpec formatted documentation from analysis results. | [spec.md](../generator/spec.md) |
| Drift | Detects and analyzes specification drift between code changes and documentation. | [spec.md](../drift/spec.md) |
| Verifier | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Verifies the accuracy of OpenSpec specifications against the actual codebase by analyzing file purposes, imports, exports, and requirement coverage. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: PipelineResult, AnalysisConfig, GenerationConfig and 10 more. | [spec.md](../types/spec.md) |
| Cli | Manages and displays progress indicators for CLI operations, including spinners and progress bars. | [spec.md](../cli/spec.md) |
| Spec | Defines core data models: OpenLoreError. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Handles compatibility and validation of OpenSpec configurations and specifications. | [spec.md](../openspec/spec.md) |
| Import | [PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses and analyzes import/export statements in JavaScript/TypeScript files. | [spec.md](../import/spec.md) |
| Chat | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Handles interactions with language model providers (Gemini, Anthropic, OpenAI-compatible) to process chat messages and tool calls. | [spec.md](../chat/spec.md) |
| Mcp-handlers | Provides utility functions for MCP tool handlers, including directory validation, error sanitization, and file path resolution. | [spec.md](../mcp-handlers/spec.md) |
| Utilities | Provides custom error handling for the openlore tool with specific error codes, user-friendly messages, and suggestions. | [spec.md](../utilities/spec.md) |
| App | Get the mapping of the codebase | [spec.md](../app/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use layered architecture for clear separation of concerns
- Implement vector indexing for semantic search capabilities
- Support multiple LLM providers for flexibility
- Use tree-sitter for accurate code parsing and analysis
- Implement a pre-commit hook for spec drift detection

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI command → API endpoint → service → analyzer → repository; results are returned to the user via CLI output or API responses
### Requirement: NorthStarIsADeterministicStructuralContextSubstrateForCodingAgents

The system SHALL provide deterministic, locally-computed structural context as a substrate for coding agents, grounding all capabilities in static analysis rather than LLM inference.

> Decision recorded: c6d1ad07
> Date: 2026-06-01

## Technical Notes

- **Architecture Style**: Layered architecture: CLI interface → API layer → service layer → repository pattern over various data sources. This pattern is chosen for its clear separation of concerns, ease of maintenance, and scalability.
- **Security Model**: API key-based authentication for LLM providers; no user authentication for CLI tool
- **External Integrations**: Git, OpenAI-compatible APIs (Gemini, Anthropic, OpenAI, etc.), LanceDB for vector indexing, tree-sitter for code parsing

## Decisions

### North star is a deterministic structural context substrate for coding agents

**Status:** Approved
**Date:** 2026-06-01
**ID:** c6d1ad07

OpenLore is positioned as local-first plumbing (like tree-sitter/SCIP/LSP) that agents build on, not a breadth product; every capability is additive to the coding-agent use case and grounded in static analysis rather than LLM guessing (Spec 13).

**Consequences:** Features must make the coding-agent case more useful or they do not ship; retrieval stays token-scoped and local-first.
