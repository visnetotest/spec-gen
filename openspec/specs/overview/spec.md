# System Overview

> Generated by spec-gen v1.0.0 on 2026-03-08 16:15
> Confidence: 90%

## Purpose

OpenSpec is a CLI tool designed to generate OpenSpec specifications from codebases using LLM
analysis. It aims to streamline the process of creating and maintaining API specifications by
leveraging advanced language models to analyze code and produce comprehensive specifications.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes call graphs and requirement mappings to identify refactoring priorities and issues in code. | [spec.md](../analyzer/spec.md) |
| Api | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides a clean interface for interacting with the local Claude Code CLI, handling error handling, retry logic, token management, and cost tracking. | [spec.md](../api/spec.md) |
| Services | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides access to various language model providers and manages token usage and cost tracking. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Takes structured LLM outputs and formats them into clean OpenSpec-compatible specification files. | [spec.md](../generator/spec.md) |
| Drift | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides git diff integration for drift detection by determining what files changed between the current working tree and a base reference. | [spec.md](../drift/spec.md) |
| Verifier | Verifies code files against specifications using LLM predictions and analysis. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: ProjectSurveyResult, ExtractedEntity, ExtractedService and 9 more. | [spec.md](../types/spec.md) |
| Cli | Configures the development server for the application. | [spec.md](../cli/spec.md) |
| Spec | Manages configuration for spec-gen and OpenSpec tools, including reading, writing, and merging configurations. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Validates the structure and content of OpenSpec specifications and configuration files. | [spec.md](../openspec/spec.md) |
| Chat | Provides a set of tools for interacting with the architecture and codebase through chat interfaces. | [spec.md](../chat/spec.md) |
| Mcp-handlers | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Provides handlers for analyzing codebase and generating various reports and summaries. | [spec.md](../mcp-handlers/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use of layered architecture to separate concerns and improve maintainability
- Integration with multiple LLM providers for flexibility and cost optimization
- Static analysis for codebase understanding and drift detection
- Bidirectional mapping between OpenSpec domains and source files for effective drift detection
- Use of graph-based analysis techniques for dependency and call graph analysis
- Token management and cost tracking for LLM interactions
- Intelligent file traversal and significance scoring to optimize LLM usage
- Generation of various analysis artifacts for comprehensive codebase understanding

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI command → API endpoint → Service layer → Analyzer → LLM → Generator → Persistence. Data flows from user input through the CLI, into the API layer, processed by services, analyzed by the analyzer, sent to the LLM for processing, and finally generated and persisted by the generator.

## Technical Notes

- **Architecture Style**: Layered architecture: CLI → API → Service → Analyzer → LLM → Generator. This pattern is chosen to separate concerns, improve maintainability, and facilitate scalability.
- **Security Model**: Authentication and authorization are not explicitly detailed in the provided analysis. It is uncertain if the system implements any security measures.
- **External Integrations**: OpenAI, Ollama, LocalAI, vLLM, LM Studio, Anthropic Claude, Google Gemini, LanceDB
