# Analyzer Specification

> Generated by spec-gen v1.0.0 on 2026-03-08 16:15
> Source files: src/api/types.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes call graphs and requirement
mappings to identify refactoring priorities and issues in code.

## Entities

### AnalyzeResult

Result of the codebase analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| repoMap | CoreRepositoryMap | Repository mapping data |
| depGraph | DependencyGraphResult | Dependency graph data |
| artifacts | AnalysisArtifacts | Generated analysis artifacts |
| duration | number | Duration of the analysis in milliseconds |

## Requirements

### Requirement: Map

The system SHALL generates a complete repository map by analyzing the repository structure, language usage, frameworks, directory statistics, and file clusters.

#### Scenario: ValidRepository
- **GIVEN** A valid repository path
- **WHEN** map is called
- **THEN** A complete repository map is generated

#### Scenario: InvalidRepository
- **GIVEN** An invalid repository path
- **WHEN** map is called
- **THEN** An error is thrown

### Requirement: Writeoutput

The system SHALL writes the generated repository map to the output directory.

#### Scenario: ValidOutputDirectory
- **GIVEN** A valid output directory
- **WHEN** writeOutput is called
- **THEN** The repository map is written to the output directory

#### Scenario: InvalidOutputDirectory
- **GIVEN** An invalid output directory
- **WHEN** writeOutput is called
- **THEN** An error is thrown


## Sub-components

> `RepositoryMapper` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Loadpackagejson

> Implements: `loadPackageJson`

Loads the package.json file from the repository root to extract project metadata

#### Requirement: Readpackagejson

The system SHALL read the package.json file from the repository root

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Parsepackagejson

The system SHALL parse the package.json content to extract project metadata

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Walk

> Implements: `walk`

Walks the codebase and collects file metadata

#### Requirement: Loadignorepatterns

The system SHALL load ignore patterns from .gitignore and other ignore files

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Walkdirectory

The system SHALL walk through the directory tree and collect file metadata, respecting ignore patterns

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Scorefiles

> Implements: `scoreFiles`

Scores all files in the repository

#### Requirement: Scorefile

The system SHALL score each file based on its content and metadata

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Push

The system SHALL push the scored file to the scored files array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectprojecttype

> Implements: `detectProjectType`

Detects the project type based on manifest files

#### Requirement: Detectprojecttype

The system SHALL detect the project type based on manifest files in the repository root

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatelanguages

> Implements: `calculateLanguages`

Calculates the language breakdown of the repository

#### Requirement: Push

The system SHALL push the language statistics to the languages array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectframeworks

> Implements: `detectFrameworks`

Detects frameworks used in the repository

#### Requirement: Push

The system SHALL push the detected frameworks to the frameworks array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatedirectorystats

> Implements: `calculateDirectoryStats`

Calculates directory statistics for the repository

#### Requirement: Push

The system SHALL push the directory statistics to the directory stats array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Clusterfiles

> Implements: `clusterFiles`

Clusters files in the repository based on their content and metadata

#### Requirement: Push

The system SHALL push the file clusters to the clusters array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Inferdomains

The system SHALL infer domains for the file clusters

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Detectlayer

The system SHALL detect layers for the file clusters

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getprojectname

> Implements: `getProjectName`

Gets the project name from the repository metadata

#### Requirement: Getprojectname

The system SHALL get the project name from the repository metadata

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Embed

The system SHALL computes embeddings for a list of texts.

#### Scenario: EmptyInput
- **GIVEN** Empty array of texts
- **WHEN** embed is called
- **THEN** Empty array of embeddings is returned

#### Scenario: SingleBatch
- **GIVEN** Array of texts with length less than or equal to batchSize
- **WHEN** embed is called
- **THEN** Embeddings for all texts are returned in the same order

#### Scenario: MultipleBatches
- **GIVEN** Array of texts with length greater than batchSize
- **WHEN** embed is called
- **THEN** Embeddings for all texts are returned in the same order

### Requirement: Fromenv

The system SHALL builds an embeddingservice from environment variables.

#### Scenario: ValidEnvironmentVariables
- **GIVEN** EMBED_BASE_URL and EMBED_MODEL environment variables are set
- **WHEN** fromEnv is called
- **THEN** EmbeddingService is created with the specified configuration

#### Scenario: MissingEmbedBaseUrl
- **GIVEN** EMBED_BASE_URL environment variable is not set
- **WHEN** fromEnv is called
- **THEN** Error is thrown

#### Scenario: MissingEmbedModel
- **GIVEN** EMBED_MODEL environment variable is not set
- **WHEN** fromEnv is called
- **THEN** Error is thrown

### Requirement: Fromconfig

The system SHALL builds an embeddingservice from a specgenconfig.

#### Scenario: ValidConfiguration
- **GIVEN** SpecGenConfig contains valid embedding configuration
- **WHEN** fromConfig is called
- **THEN** EmbeddingService is created with the specified configuration

#### Scenario: MissingConfiguration
- **GIVEN** SpecGenConfig does not contain embedding configuration
- **WHEN** fromConfig is called
- **THEN** null is returned

### Requirement: Getfilegodfunctions

The system SHALL returns all god functions (fanout >= threshold) defined in a given file.

#### Scenario: GodFunctionsFound
- **GIVEN** A file with functions having fanOut >= threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an array of FunctionNode objects representing the god functions

#### Scenario: NoGodFunctionsFound
- **GIVEN** A file with no functions having fanOut >= threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an empty array

### Requirement: Extractsubgraph

The system SHALL extracts a depth-limited subgraph around a root node, following only outgoing (callee) edges.

#### Scenario: SubgraphExtractedSuccessfully
- **GIVEN** A valid callGraph and root FunctionNode
- **WHEN** extractSubgraph is called
- **THEN** Returns a SubGraph object containing the root node, reachable nodes, and edges

#### Scenario: SubgraphExtractionLimitedByDepth
- **GIVEN** A callGraph with nodes beyond MAX_DEPTH from the root
- **WHEN** extractSubgraph is called
- **THEN** Returns a SubGraph object containing nodes only up to MAX_DEPTH from the root

#### Scenario: SubgraphExtractionLimitedByNodeCount
- **GIVEN** A callGraph with more nodes than MAX_NODES
- **WHEN** extractSubgraph is called
- **THEN** Returns a SubGraph object containing only MAX_NODES nodes

### Requirement: Formatsubgraph

The system SHALL formats a single subgraph as a compact ascii block for llm prompts.

#### Scenario: SubgraphFormattedSuccessfully
- **GIVEN** A valid SubGraph object
- **WHEN** formatSubgraph is called
- **THEN** Returns a formatted string representing the subgraph

### Requirement: Buildgraphpromptsection

The system SHALL builds a graph-based prompt section for a large file. returns null if no call-graph data is available for the file, signaling the caller to fall back to raw source chunking.

#### Scenario: GraphPromptSectionBuiltSuccessfully
- **GIVEN** Valid callGraph and signatures data for the file
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns a formatted string representing the graph-based prompt section

#### Scenario: NoCallGraphDataAvailable
- **GIVEN** No callGraph data for the file
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns null

#### Scenario: NoGodFunctionsFound
- **GIVEN** A file with no god functions
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns null

### Requirement: Inferclusterrole

The system SHALL infers a module cluster's architectural role from aggregate metrics.

#### Scenario: EntryLayer
- **GIVEN** Entry count is more than half of file count
- **WHEN** inferClusterRole is called
- **THEN** Returns 'entry_layer'

#### Scenario: Orchestrator
- **GIVEN** Both hub and entry counts are greater than zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'orchestrator'

#### Scenario: CoreUtilities
- **GIVEN** Hub count is greater than zero and entry count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'core_utilities'

#### Scenario: ApiLayer
- **GIVEN** Entry count is greater than zero and hub count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'api_layer'

#### Scenario: Internal
- **GIVEN** Neither hub nor entry counts are greater than zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'internal'

### Requirement: Buildarchitectureoverview

The system SHALL builds an architectureoverview from pre-loaded dependency graph and llm context.

#### Scenario: ValidInputs
- **GIVEN** Both depGraph and ctx are non-null
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with all fields populated

#### Scenario: MissingDepgraph
- **GIVEN** depGraph is null and ctx is non-null
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with summary and globalEntryPoints populated

#### Scenario: MissingCtx
- **GIVEN** ctx is null and depGraph is non-null
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with summary and clusters populated

### Requirement: Renderarchitecturemarkdown

The system SHALL renders an architectureoverview as a markdown document.

#### Scenario: ValidOverview
- **GIVEN** overview is a valid ArchitectureOverview
- **WHEN** renderArchitectureMarkdown is called
- **THEN** Returns a string containing the rendered Markdown document

### Requirement: Build

The system SHALL builds the dependency graph from scored files.

#### Scenario: ValidFiles
- **GIVEN** Array of scored files
- **WHEN** build is called
- **THEN** Dependency graph is built successfully

#### Scenario: EmptyFiles
- **GIVEN** Empty array of scored files
- **WHEN** build is called
- **THEN** Empty dependency graph is returned


## Sub-components

> `DependencyGraphBuilder` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Clear

> Implements: `clear`

Clears the dependency graph and all related data structures

#### Requirement: Cleargraph

The system SHALL the system shall clear the dependency graph, removing all nodes and edges.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Cleardatastructures

The system SHALL the system shall clear all related data structures, such as degrees, betweenness, page rank, clusters, cycles, rankings, and statistics.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Parsefiles

> Implements: `parseFiles`

Parses multiple files and returns a map of file paths to their analysis results

#### Requirement: Parsemultiplefiles

The system SHALL the system shall parse multiple files and return a map of file paths to their analysis results.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Buildedges

> Implements: `buildEdges`

Builds edges in the dependency graph based on file analysis results

#### Requirement: Resolveimports

The system SHALL the system shall resolve imports for each file and build edges in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatedegrees

> Implements: `calculateDegrees`

Calculates the in-degree and out-degree for each node in the dependency graph

#### Requirement: Calculatenodedegrees

The system SHALL the system shall calculate the in-degree and out-degree for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatebetweenness

> Implements: `calculateBetweenness`

Calculates the betweenness centrality for each node in the dependency graph

#### Requirement: Calculatenodebetweenness

The system SHALL the system shall calculate the betweenness centrality for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatepagerank

> Implements: `calculatePageRank`

Calculates the PageRank for each node in the dependency graph

#### Requirement: Calculatenodepagerank

The system SHALL the system shall calculate the pagerank for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectclusters

> Implements: `detectClusters`

Detects clusters in the dependency graph and suggests domain names for each cluster

#### Requirement: Detectgraphclusters

The system SHALL the system shall detect clusters in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Suggestclusterdomainnames

The system SHALL the system shall suggest domain names for each cluster based on the node ids in the cluster.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectcycles

> Implements: `detectCycles`

Detects cycles in the dependency graph

#### Requirement: Detectgraphcycles

The system SHALL the system shall detect cycles in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Generaterankings

> Implements: `generateRankings`

Generates rankings for nodes in the dependency graph based on various metrics

#### Requirement: Generatenoderankings

The system SHALL the system shall generate rankings for nodes in the dependency graph based on various metrics such as degree, betweenness, and pagerank.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatestatistics

> Implements: `calculateStatistics`

Calculates statistics for the dependency graph such as average degree, average betweenness, and average PageRank

#### Requirement: Calculategraphstatistics

The system SHALL the system shall calculate statistics for the dependency graph such as average degree, average betweenness, and average pagerank.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Tod3format

The system SHALL converts graph to d3.js force graph format.

#### Scenario: ValidGraph
- **GIVEN** Dependency graph result
- **WHEN** toD3Format is called
- **THEN** Graph is converted to D3.js force graph format

### Requirement: Tomermaidformat

The system SHALL converts graph to mermaid diagram syntax.

#### Scenario: ValidGraph
- **GIVEN** Dependency graph result
- **WHEN** toMermaidFormat is called
- **THEN** Graph is converted to Mermaid diagram syntax

#### Scenario: LargeGraph
- **GIVEN** Dependency graph result with more nodes than maxNodes
- **WHEN** toMermaidFormat is called
- **THEN** Graph is truncated to maxNodes

### Requirement: Todotformat

The system SHALL converts graph to dot format (graphviz).

#### Scenario: ValidGraph
- **GIVEN** Dependency graph result
- **WHEN** toDotFormat is called
- **THEN** Graph is converted to DOT format


## Sub-components

> `DependencyGraphConverter` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Clear

> Implements: `clear`

Clears the dependency graph and all related data structures.

#### Requirement: Cleargraph

The system SHALL the system shall clear the dependency graph and all related data structures.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Parsefiles

> Implements: `parseFiles`

Parses multiple files and returns a map of file paths to their analysis results.

#### Requirement: Parsemultiplefiles

The system SHALL the system shall parse multiple files and return a map of file paths to their analysis results.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Buildedges

> Implements: `buildEdges`

Builds edges in the dependency graph based on file analysis results.

#### Requirement: Resolveimports

The system SHALL the system shall resolve imports for each file and build edges in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatedegrees

> Implements: `calculateDegrees`

Calculates the in-degree and out-degree for each node in the dependency graph.

#### Requirement: Calculatenodedegrees

The system SHALL the system shall calculate the in-degree and out-degree for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatebetweenness

> Implements: `calculateBetweenness`

Calculates the betweenness centrality for each node in the dependency graph.

#### Requirement: Calculatenodebetweenness

The system SHALL the system shall calculate the betweenness centrality for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatepagerank

> Implements: `calculatePageRank`

Calculates the PageRank for each node in the dependency graph.

#### Requirement: Calculatenodepagerank

The system SHALL the system shall calculate the pagerank for each node in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectclusters

> Implements: `detectClusters`

Detects clusters in the dependency graph and suggests domain names for each cluster.

#### Requirement: Detectgraphclusters

The system SHALL the system shall detect clusters in the dependency graph and suggest domain names for each cluster.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectcycles

> Implements: `detectCycles`

Detects cycles in the dependency graph.

#### Requirement: Detectgraphcycles

The system SHALL the system shall detect cycles in the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Generaterankings

> Implements: `generateRankings`

Generates rankings for nodes in the dependency graph based on various metrics.

#### Requirement: Generatenoderankings

The system SHALL the system shall generate rankings for nodes in the dependency graph based on various metrics.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Calculatestatistics

> Implements: `calculateStatistics`

Calculates various statistics for the dependency graph.

#### Requirement: Calculategraphstatistics

The system SHALL the system shall calculate various statistics for the dependency graph.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Walk

The system SHALL traverses the codebase starting from the root path, collecting file metadata and respecting ignore patterns.

#### Scenario: SuccessfulTraversal
- **GIVEN** A valid root path and options
- **WHEN** walk is called
- **THEN** File metadata is collected and returned

#### Scenario: AbortedTraversal
- **GIVEN** An abort signal is triggered
- **WHEN** walk is called
- **THEN** Traversal is aborted and partial results are returned

### Requirement: Hasshebang

The system SHALL checks if a file has a shebang line.

#### Scenario: FileHasShebang
- **GIVEN** A file with a shebang line
- **WHEN** hasShebang is called
- **THEN** Returns true

#### Scenario: FileDoesNotHaveShebang
- **GIVEN** A file without a shebang line
- **WHEN** hasShebang is called
- **THEN** Returns false

### Requirement: Countlines

The system SHALL counts the lines in a file. returns -1 for files larger than max_read_size.

#### Scenario: FileIsWithinSizeLimit
- **GIVEN** A file smaller than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns the line count

#### Scenario: FileExceedsSizeLimit
- **GIVEN** A file larger than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns -1

### Requirement: Istestfile

The system SHALL checks if a file path matches test patterns.

#### Scenario: FileIsATestFile
- **GIVEN** A file path matching test patterns
- **WHEN** isTestFile is called
- **THEN** Returns true

#### Scenario: FileIsNotATestFile
- **GIVEN** A file path not matching test patterns
- **WHEN** isTestFile is called
- **THEN** Returns false

### Requirement: Isconfigfile

The system SHALL checks if a file is a configuration file.

#### Scenario: FileIsAConfigFile
- **GIVEN** A file name matching config patterns
- **WHEN** isConfigFile is called
- **THEN** Returns true

#### Scenario: FileIsNotAConfigFile
- **GIVEN** A file name not matching config patterns
- **WHEN** isConfigFile is called
- **THEN** Returns false

### Requirement: Isgeneratedfile

The system SHALL checks if a file is likely generated.

#### Scenario: FileIsGenerated
- **GIVEN** A file name or path matching generated patterns
- **WHEN** isGeneratedFile is called
- **THEN** Returns true

#### Scenario: FileIsNotGenerated
- **GIVEN** A file name or path not matching generated patterns
- **WHEN** isGeneratedFile is called
- **THEN** Returns false

### Requirement: Isentrypoint

The system SHALL checks if a file might be an entry point.

#### Scenario: FileIsAnEntryPoint
- **GIVEN** A file name, path, or shebang matching entry point patterns
- **WHEN** isEntryPoint is called
- **THEN** Returns true

#### Scenario: FileIsNotAnEntryPoint
- **GIVEN** A file name, path, or shebang not matching entry point patterns
- **WHEN** isEntryPoint is called
- **THEN** Returns false

### Requirement: Loadignorepatterns

The system SHALL loads and combines ignore patterns from various sources.

#### Scenario: PatternsLoadedSuccessfully
- **GIVEN** A valid root path with .gitignore and .spec-gen-ignore files
- **WHEN** loadIgnorePatterns is called
- **THEN** Returns combined ignore patterns

#### Scenario: PatternsLoadedPartially
- **GIVEN** A valid root path with only one ignore file
- **WHEN** loadIgnorePatterns is called
- **THEN** Returns combined ignore patterns from available files

### Requirement: Computedepths

The system SHALL calculates the depth of each function node from the nearest entry point using bfs.

#### Scenario: EntryPoints
- **GIVEN** Nodes with fanIn === 0
- **WHEN** computeDepths is called
- **THEN** Depths are set to 0

#### Scenario: UnreachableNodes
- **GIVEN** Nodes not reachable from any entry point
- **WHEN** computeDepths is called
- **THEN** Depths are set to -1

### Requirement: Computesccs

The system SHALL computes strongly connected components (sccs) in the call graph using tarjan's algorithm.

#### Scenario: CyclicDependencies
- **GIVEN** Nodes with cyclic dependencies
- **WHEN** computeSCCs is called
- **THEN** SCCs are identified and mapped

### Requirement: Analyzeforrefactoring

The system SHALL analyzes a call graph for refactoring priorities and issues.

#### Scenario: HighFanInNodes
- **GIVEN** Nodes with fanIn >= HIGH_FAN_IN
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as high_fan_in

#### Scenario: HighFanOutNodes
- **GIVEN** Nodes with fanOut >= HIGH_FAN_OUT
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as high_fan_out

#### Scenario: MultiRequirementNodes
- **GIVEN** Nodes implementing > SRP_MAX_REQUIREMENTS
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as multi_requirement

#### Scenario: UnreachableNodes
- **GIVEN** Nodes not reachable from any entry point
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as unreachable

#### Scenario: NodesInCycles
- **GIVEN** Nodes part of a cyclic dependency
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as in_cycle

#### Scenario: NodesInCloneGroups
- **GIVEN** Nodes appearing in duplicate code clone groups
- **WHEN** analyzeForRefactoring is called
- **THEN** Nodes are flagged as in_clone_group

### Requirement: Build

The system SHALL builds or rebuilds the vector index from call graph nodes and signatures.

#### Scenario: SuccessfulBuild
- **GIVEN** Valid input parameters and no existing index
- **WHEN** build is called
- **THEN** A new vector index is created and stored in the specified output directory

#### Scenario: BuildWithExistingIndex
- **GIVEN** Valid input parameters and an existing index
- **WHEN** build is called
- **THEN** The existing index is overwritten with the new index

#### Scenario: BuildWithNoFunctions
- **GIVEN** Empty nodes array
- **WHEN** build is called
- **THEN** An error is thrown indicating no functions to index

#### Scenario: BuildWithEmbeddingMismatch
- **GIVEN** Valid input parameters but embedding count mismatch
- **WHEN** build is called
- **THEN** An error is thrown indicating embedding count mismatch

### Requirement: Search

The system SHALL performs semantic search over the index and returns up to `limit` results sorted by similarity.

#### Scenario: SuccessfulSearch
- **GIVEN** Valid query and existing index
- **WHEN** search is called
- **THEN** Up to `limit` search results are returned sorted by similarity

#### Scenario: SearchWithNoIndex
- **GIVEN** Valid query but no existing index
- **WHEN** search is called
- **THEN** An error is thrown indicating the index does not exist

#### Scenario: SearchWithFailedEmbedding
- **GIVEN** Valid query but failed to embed the query
- **WHEN** search is called
- **THEN** An error is thrown indicating failed to embed query

#### Scenario: SearchWithFilters
- **GIVEN** Valid query, existing index, and filters (language, minFanIn)
- **WHEN** search is called
- **THEN** Up to `limit` search results are returned sorted by similarity and filtered by the specified
criteria

### Requirement: Exists

The system SHALL checks if a vector index has been built for the specified output directory.

#### Scenario: IndexExists
- **GIVEN** An existing vector index in the specified output directory
- **WHEN** exists is called
- **THEN** Returns true

#### Scenario: IndexDoesNotExist
- **GIVEN** No vector index in the specified output directory
- **WHEN** exists is called
- **THEN** Returns false

### Requirement: Parsefile

The system SHALL reads a file and parses its imports and exports.

#### Scenario: ValidFile
- **GIVEN** A valid file path
- **WHEN** parseFile is called
- **THEN** FileAnalysis is returned with imports and exports

#### Scenario: InvalidFile
- **GIVEN** An invalid file path
- **WHEN** parseFile is called
- **THEN** An error is thrown

### Requirement: Parseimports

The system SHALL parses imports from file content.

#### Scenario: ValidImports
- **GIVEN** Content with valid imports
- **WHEN** parseImports is called
- **THEN** ImportInfo array is returned

#### Scenario: NoImports
- **GIVEN** Content with no imports
- **WHEN** parseImports is called
- **THEN** Empty array is returned

### Requirement: Parseexports

The system SHALL parses exports from file content.

#### Scenario: ValidExports
- **GIVEN** Content with valid exports
- **WHEN** parseExports is called
- **THEN** ExportInfo array is returned

#### Scenario: NoExports
- **GIVEN** Content with no exports
- **WHEN** parseExports is called
- **THEN** Empty array is returned

### Requirement: Resolveimport

The system SHALL resolves an import source to an absolute path.

#### Scenario: RelativeImport
- **GIVEN** A relative import source
- **WHEN** resolveImport is called
- **THEN** Absolute path is returned

#### Scenario: PackageImport
- **GIVEN** A package import source
- **WHEN** resolveImport is called
- **THEN** Package path is returned

#### Scenario: BuiltinImport
- **GIVEN** A builtin import source
- **WHEN** resolveImport is called
- **THEN** Builtin path is returned

### Requirement: Parseexports

The system SHALL parses export statements from javascript/typescript content.

#### Scenario: DefaultExport
- **GIVEN** Content with export default X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: NamedExport
- **GIVEN** Content with export { X, Y }
- **WHEN** parseExports is called
- **THEN** ExportInfo for each name with correct name, isDefault, and flags is returned

#### Scenario: ReExport
- **GIVEN** Content with export { X } from 'module'
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, reExportSource, and flags is returned

#### Scenario: VariableExport
- **GIVEN** Content with export const X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: FunctionExport
- **GIVEN** Content with export function X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: ClassExport
- **GIVEN** Content with export class X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: TypeExport
- **GIVEN** Content with export type X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: InterfaceExport
- **GIVEN** Content with export interface X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: EnumExport
- **GIVEN** Content with export enum X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: ReExportAll
- **GIVEN** Content with export * from 'module'
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, reExportSource, and flags is returned

#### Scenario: CommonjsModule.exports
- **GIVEN** Content with module.exports = X
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

#### Scenario: CommonjsExports.x
- **GIVEN** Content with exports.X = Y
- **WHEN** parseExports is called
- **THEN** ExportInfo with correct name, isDefault, and flags is returned

### Requirement: Clearcache

The system SHALL clears the cache of parsed files.

#### Scenario: CacheCleared
- **GIVEN** Cache contains parsed files
- **WHEN** clearCache is called
- **THEN** Cache is empty

### Requirement: Getfiletype

The system SHALL determines the file type based on the file extension.

#### Scenario: TypescriptFile
- **GIVEN** File path ends with .ts
- **WHEN** getFileType is called
- **THEN** Returns 'ts'

#### Scenario: JavascriptFile
- **GIVEN** File path ends with .js
- **WHEN** getFileType is called
- **THEN** Returns 'js'

#### Scenario: PythonFile
- **GIVEN** File path ends with .py
- **WHEN** getFileType is called
- **THEN** Returns 'python'

#### Scenario: UnknownFile
- **GIVEN** File path ends with an unsupported extension
- **WHEN** getFileType is called
- **THEN** Returns 'unknown'

### Requirement: Parsefile

The system SHALL parses a single file to extract import and export statements.

#### Scenario: FileParsedSuccessfully
- **GIVEN** File exists and is supported
- **WHEN** parseFile is called
- **THEN** Returns FileAnalysis object with imports and exports

#### Scenario: FileNotFound
- **GIVEN** File does not exist
- **WHEN** parseFile is called
- **THEN** Returns FileAnalysis object with parse error

#### Scenario: UnsupportedFileType
- **GIVEN** File has an unsupported extension
- **WHEN** parseFile is called
- **THEN** Returns FileAnalysis object with parse error

### Requirement: Parsefiles

The system SHALL parses multiple files to extract import and export statements.

#### Scenario: FilesParsedSuccessfully
- **GIVEN** Files exist and are supported
- **WHEN** parseFiles is called
- **THEN** Returns Map with FileAnalysis objects for each file

#### Scenario: SomeFilesNotFound
- **GIVEN** Some files do not exist
- **WHEN** parseFiles is called
- **THEN** Returns Map with FileAnalysis objects for existing files and parse errors for missing files

#### Scenario: UnsupportedFileTypes
- **GIVEN** Files have unsupported extensions
- **WHEN** parseFiles is called
- **THEN** Returns Map with FileAnalysis objects with parse errors for unsupported files

### Requirement: Detectlanguage

The system SHALL detects the programming language of a file based on its extension.

#### Scenario: PythonFile
- **GIVEN** A file with a .py extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'Python'

#### Scenario: TypescriptFile
- **GIVEN** A file with a .ts extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'TypeScript'

#### Scenario: UnknownFile
- **GIVEN** A file with an unknown extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'unknown'

### Requirement: Extractpython

The system SHALL extracts function/class signatures and docstrings from python source files.

#### Scenario: ClassDeclaration
- **GIVEN** A Python file with a class declaration
- **WHEN** extractPython is called
- **THEN** Returns an array of ExtractedSignature objects for the class and its methods

#### Scenario: FunctionDeclaration
- **GIVEN** A Python file with a function declaration
- **WHEN** extractPython is called
- **THEN** Returns an array of ExtractedSignature objects for the function

#### Scenario: DocstringExtraction
- **GIVEN** A Python file with a function or class declaration and a docstring
- **WHEN** extractPython is called
- **THEN** Returns an ExtractedSignature object with the docstring included

### Requirement: Extracttypescript

The system SHALL extracts function/class signatures and docstrings from typescript/javascript source files.

#### Scenario: ClassDeclaration
- **GIVEN** A TypeScript file with a class declaration
- **WHEN** extractTypeScript is called
- **THEN** Returns an array of ExtractedSignature objects for the class

#### Scenario: InterfaceDeclaration
- **GIVEN** A TypeScript file with an interface declaration
- **WHEN** extractTypeScript is called
- **THEN** Returns an array of ExtractedSignature objects for the interface

#### Scenario: FunctionDeclaration
- **GIVEN** A TypeScript file with a function declaration
- **WHEN** extractTypeScript is called
- **THEN** Returns an array of ExtractedSignature objects for the function

### Requirement: Generatellmcontext

The system SHALL generates llm context from repository map and dependency graph.

#### Scenario: ValidRepositoryMapAndDependencyGraph
- **GIVEN** Valid repository map and dependency graph
- **WHEN** generateLLMContext is called
- **THEN** LLMContext is generated

#### Scenario: InvalidRepositoryMapAndDependencyGraph
- **GIVEN** Invalid repository map and dependency graph
- **WHEN** generateLLMContext is called
- **THEN** Error is thrown

### Requirement: Istestfile

The system SHALL heuristic to detect test/spec files across languages.

#### Scenario: TestFile
- **GIVEN** File path of a test file
- **WHEN** isTestFile is called
- **THEN** True is returned

#### Scenario: NonTestFile
- **GIVEN** File path of a non-test file
- **WHEN** isTestFile is called
- **THEN** False is returned


## Sub-components

> `AnalysisArtifactGenerator` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Istestfile

> Implements: `isTestFile`

Heuristically detect test/spec files across languages.

#### Requirement: Testfiledetection

The system SHALL the function shall return true if the file path matches known test/spec file patterns for various languages.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Push

> Implements: `push`

Push data to a specified target.

#### Requirement: Datapush

The system SHALL the function shall push the provided data to the specified target.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extractsignatures

> Implements: `extractSignatures`

Extract function signatures from source code files.

#### Requirement: Signatureextraction

The system SHALL the function shall extract function signatures from the provided file content based on the detected language.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectlanguage

> Implements: `detectLanguage`

Detect the programming language of a file based on its extension.

#### Requirement: Languagedetection

The system SHALL the function shall detect the programming language of the file based on its extension.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Build

> Implements: `build`

Build the dependency graph from scored files.

#### Requirement: Graphconstruction

The system SHALL the function shall build the dependency graph from the provided scored files.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Serializecallgraph

> Implements: `serializeCallGraph`

Serialize the call graph result into a structured format.

#### Requirement: Callgraphserialization

The system SHALL the function shall serialize the provided call graph result into a structured format.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectduplicates

> Implements: `detectDuplicates`

Detect duplicate code across files using various normalization and comparison techniques.

#### Requirement: Duplicatedetection

The system SHALL the function shall detect duplicate code across files using various normalization and comparison techniques.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Analyzeforrefactoring

> Implements: `analyzeForRefactoring`

Analyze the codebase for potential refactoring opportunities.

#### Requirement: Refactoringanalysis

The system SHALL the function shall analyze the codebase for potential refactoring opportunities.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Build

The system SHALL constructs a call graph for a given project by parsing source files and detecting function calls.

#### Scenario: PythonProject
- **GIVEN** A project with Python source files
- **WHEN** build is called
- **THEN** A call graph is constructed using Python-specific parsing

#### Scenario: TypescriptProject
- **GIVEN** A project with TypeScript source files
- **WHEN** build is called
- **THEN** A call graph is constructed using TypeScript-specific parsing

### Requirement: Detectlayerviolations

The system SHALL analyzes the call graph to detect violations of architectural layering rules.

#### Scenario: LayerViolationDetected
- **GIVEN** A call graph with a function in a lower layer calling a function in a higher layer
- **WHEN** detectLayerViolations is called
- **THEN** A layer violation is detected and reported


## Sub-components

> `CallGraphBuilder` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Extractpygraph

> Implements: `extractPyGraph`

Extracts a call graph from Python code

#### Requirement: Parsepythoncode

The system SHALL the function shall parse python code using the getpyparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extracttsgraph

> Implements: `extractTSGraph`

Extracts a call graph from TypeScript code

#### Requirement: Parsetypescriptcode

The system SHALL the function shall parse typescript code using the gettsparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extractgograph

> Implements: `extractGoGraph`

Extracts a call graph from Go code

#### Requirement: Parsegocode

The system SHALL the function shall parse go code using the getgoparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extractrustgraph

> Implements: `extractRustGraph`

Extracts a call graph from Rust code

#### Requirement: Parserustcode

The system SHALL the function shall parse rust code using the getrustparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extractrubygraph

> Implements: `extractRubyGraph`

Extracts a call graph from Ruby code

#### Requirement: Parserubycode

The system SHALL the function shall parse ruby code using the getrubyparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Extractjavagraph

> Implements: `extractJavaGraph`

Extracts a call graph from Java code

#### Requirement: Parsejavacode

The system SHALL the function shall parse java code using the getjavaparser method to generate an abstract syntax tree (ast)

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Buildcallgraph

The system SHALL the function shall build a call graph by traversing the ast and using the push method to add nodes and edges to the graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Push

> Implements: `push`

Adds nodes and edges to the call graph

#### Requirement: Addnode

The system SHALL the function shall add a node to the call graph for each function or method encountered in the code

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Addedge

The system SHALL the function shall add an edge to the call graph for each call made by a function or method

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Detectlayerviolations

> Implements: `detectLayerViolations`

Detects architectural layer violations in the call graph

#### Requirement: Getlayer

The system SHALL the function shall determine the architectural layer of each node in the call graph

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Checklayerviolations

The system SHALL the function shall check for layer violations by ensuring that edges only go from higher layers to lower layers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Detectduplicates

The system SHALL analyzes code files to identify duplicate code segments using shingling and jaccard similarity algorithms.

#### Scenario: IdentifyDuplicates
- **GIVEN** A codebase with duplicate code segments
- **WHEN** detectDuplicates is called
- **THEN** Duplicate code segments are identified and grouped


## Sub-components

> `DuplicateDetector` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Byteoffsettoline

> Implements: `byteOffsetToLine`

Converts byte offsets to line numbers in a source code file

#### Requirement: Convertoffsettoline

The system SHALL convert a given byte offset to its corresponding line number in the source code

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Normalizetype1

> Implements: `normalizeType1`

Normalizes code segments by removing comments and standardizing whitespace

#### Requirement: Removecomments

The system SHALL remove all comments from the input code segment

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Standardizewhitespace

The system SHALL standardize whitespace in the input code segment by replacing all whitespace characters with a single space

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Normalizetype2

> Implements: `normalizeType2`

Further normalizes code segments by applying additional transformations

#### Requirement: Applytransformations

The system SHALL apply additional transformations to the input code segment, such as replacing variable names with generic placeholders

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Tokenize

> Implements: `tokenize`

Tokenizes a code segment into individual tokens for further analysis

#### Requirement: Tokenizecode

The system SHALL tokenize the input code segment into individual tokens, such as keywords, identifiers, and literals

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Push

> Implements: `push`

Adds a new element to the end of an array

#### Requirement: Addelement

The system SHALL add the input element to the end of the specified array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Sha16

> Implements: `sha16`

Computes the SHA-1 hash of a given input and returns the first 16 characters of the hash

#### Requirement: Computehash

The system SHALL compute the sha-1 hash of the input and return the first 16 characters of the hash

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Getshingles

> Implements: `getShingles`

Generates shingles (contiguous sequences of tokens) from a tokenized code segment

#### Requirement: Generateshingles

The system SHALL generate shingles of the specified size from the input tokenized code segment

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Jaccard

> Implements: `jaccard`

Computes the Jaccard similarity coefficient between two sets of shingles

#### Requirement: Computesimilarity

The system SHALL compute the jaccard similarity coefficient between the two input sets of shingles

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Calculatenamescore

The system SHALL calculates the name-based score for a file based on its name and custom scoring rules.

#### Scenario: HighValueName
- **GIVEN** File name contains a high-value pattern
- **WHEN** calculateNameScore is called
- **THEN** Score is set to the corresponding high-value points

#### Scenario: NegativeName
- **GIVEN** File name contains a negative pattern
- **WHEN** calculateNameScore is called
- **THEN** Score is adjusted by the corresponding negative points

#### Scenario: NoMatchingPatterns
- **GIVEN** File name does not contain any matching patterns
- **WHEN** calculateNameScore is called
- **THEN** Score is 0

### Requirement: Calculatepathscore

The system SHALL calculates the path-based score for a file based on its relative path, depth, and custom scoring rules.

#### Scenario: RootDirectory
- **GIVEN** File is in the root directory
- **WHEN** calculatePathScore is called
- **THEN** Score is increased by 15 points

#### Scenario: HighValuePath
- **GIVEN** File path contains a high-value pattern
- **WHEN** calculatePathScore is called
- **THEN** Score is set to the corresponding high-value points

#### Scenario: DeeplyNestedFile
- **GIVEN** File is deeply nested
- **WHEN** calculatePathScore is called
- **THEN** Score is decreased by 10 points

#### Scenario: DeeplyNestedUtils/helpers
- **GIVEN** File is a utils/helpers file and is deeply nested
- **WHEN** calculatePathScore is called
- **THEN** Score is decreased by 5 points

#### Scenario: NoMatchingPatterns
- **GIVEN** File path does not contain any matching patterns
- **WHEN** calculatePathScore is called
- **THEN** Score is 0

### Requirement: Calculatestructurescore

The system SHALL calculates the structure-based score for a file based on its content and number of lines.

#### Scenario: ClassDefinitions
- **GIVEN** File contains class definitions
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 10 points

#### Scenario: Interface/typeDefinitions
- **GIVEN** File contains interface/type definitions
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 15 points

#### Scenario: ExportStatements
- **GIVEN** File contains export statements
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 5 points

#### Scenario: Decorators
- **GIVEN** File contains decorators
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 15 points

#### Scenario: ManyImports
- **GIVEN** File contains many import statements
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 10 points

#### Scenario: FileSize
- **GIVEN** File size is between 50 and 500 lines
- **WHEN** calculateStructureScore is called
- **THEN** Score is increased by 5 points

#### Scenario: LargeFile
- **GIVEN** File size is greater than 1000 lines
- **WHEN** calculateStructureScore is called
- **THEN** Score is decreased by 5 points

#### Scenario: FileReadError
- **GIVEN** File cannot be read
- **WHEN** calculateStructureScore is called
- **THEN** Score is 0

### Requirement: Buildfilerelationships

The system SHALL builds a map of file relationships based on import analysis.

#### Scenario: FilesProvided
- **GIVEN** Files are provided
- **WHEN** buildFileRelationships is called
- **THEN** A map of file relationships is created

### Requirement: Calculateconnectivityscore

The system SHALL calculates the connectivity score for a file based on its import relationships.

#### Scenario: ImportedByManyFiles
- **GIVEN** File is imported by many files
- **WHEN** calculateConnectivityScore is called
- **THEN** Score is increased by 20 points

#### Scenario: ImportedByFewFiles
- **GIVEN** File is imported by few files
- **WHEN** calculateConnectivityScore is called
- **THEN** Score is increased by 10 points

#### Scenario: ImportsManyFiles
- **GIVEN** File imports many files
- **WHEN** calculateConnectivityScore is called
- **THEN** Score is increased by 10 points

#### Scenario: OrphanFile
- **GIVEN** File is not imported or imports any files
- **WHEN** calculateConnectivityScore is called
- **THEN** Score is decreased by 10 points

#### Scenario: NoRelationships
- **GIVEN** File has no relationships
- **WHEN** calculateConnectivityScore is called
- **THEN** Score is 0

## Technical Notes

- **Implementation**: `src/api/types.ts`
- **Dependencies**: SpecGenConfig, CallGraph, SignatureExtractor, DependencyGraphResult, LLMContext, ScoredFile, node:fs/promises, node:path, ignore, DuplicateDetector, @lancedb/lancedb, EmbeddingService, FunctionNode, FileSignatureMap, fs/promises, path, fs, RepositoryMap, DependencyGraph, Project, CallEdge, LayerViolation, CallGraphResult, File, FileMetadata, FileRelationship
